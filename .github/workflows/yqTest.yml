name: Check yq
on:
  workflow_dispatch:

jobs:
  yqCheck:
      runs-on: ubuntu-latest
      steps:
        - name: Display the yml content
          run: |
            cd ../../Env.yml
            yq Env.yml
